{
  "name": "@koppajs/koppajs-router",
  "version": "0.1.0",
  "description": "Reusable History API router primitives for KoppaJS applications.",
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@10.17.1",
  "files": [
    "dist",
    "README.md",
    "package.json"
  ],
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "build": "pnpm run clean && tsc -p tsconfig.build.json",
    "check:package": "node -e \"const fs=require('node:fs'); const pkg=JSON.parse(fs.readFileSync('package.json','utf8')); const entry=pkg.exports['.']; const paths=[pkg.main,pkg.module,pkg.types,entry.import,entry.types]; const missing=paths.filter((file)=>!fs.existsSync(file)); if(missing.length>0){ console.error('Missing package entrypoints:', missing.join(', ')); process.exit(1); }\"",
    "lint": "pnpm run lint:ts && pnpm run format:check",
    "lint:fix": "pnpm run lint:ts -- --fix && pnpm run format",
    "lint:ts": "eslint . --ext .ts --max-warnings=0",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:unit": "vitest run --config vitest.config.ts --coverage",
    "test:unit:watch": "vitest --config vitest.config.ts",
    "test:coverage": "pnpm run test:unit",
    "test": "pnpm run typecheck && pnpm run test:unit",
    "check": "pnpm run lint && pnpm run test && pnpm run build && pnpm run check:package"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "@typescript-eslint/eslint-plugin": "^8.44.0",
    "@typescript-eslint/parser": "^8.44.0",
    "@vitest/coverage-v8": "^3.2.4",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^10.1.8",
    "jsdom": "^26.1.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  },
  "keywords": [
    "koppajs",
    "router",
    "history-api",
    "spa",
    "typescript"
  ]
}
